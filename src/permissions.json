{
  "permissions": {
    "read:health": "Read health status",
    "start:sensor_uploads": "Start sensor data upload",
    "upload:sensor_uploads": "Upload sensor data chunks",
    "abort:sensor_uploads": "Abort sensor data upload"
  }
}
